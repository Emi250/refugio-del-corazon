---
import Base from '~/layouts/Base.astro';
import BigHeader from '~/components/BigHeader.astro';
import CTABlock from '~/components/CTABlock.astro';
import Lightbox from '~/components/Lightbox.astro';
import { getCollection } from 'astro:content';
import { t, localizedPath } from '~/i18n';

const lang = 'en' as const;
const units = await getCollection('unidades', ({ data }) => data.lang === 'en');
const sorted = units.sort((a, b) => a.data.order - b.data.order);

function midPositions(n: number): Set<number> {
  const midsNeeded = (3 - (n % 3)) % 3;
  if (midsNeeded === 0) return new Set();
  const candidates = Array.from({ length: n - 1 }, (_, i) => i + 1);
  const step = Math.floor(candidates.length / (midsNeeded + 1));
  const picks = new Set<number>();
  for (let k = 1; k <= midsNeeded; k++) picks.add(candidates[k * step] ?? candidates[candidates.length - k]);
  return picks;
}
---
<Base
  title="Photo gallery · Apartments in Capilla del Monte | Refugio del Corazón"
  description="Photos of the 4 short-term rental apartments in Capilla del Monte: interiors, exteriors, kitchen, bedrooms and shared courtyard."
>
  <BigHeader
    kicker="04 · GALLERY"
    title="Every corner"
    meta="REAL PHOTOS · NO FILTERS"
    blurb="A walk through the four units, the outdoors and the details that set the atmosphere of the refuge."
  />

  {sorted.map(unit => (
    <section class="gal-section" id={unit.slug.replace(/^en\//, '')}>
      <header class="gal-head">
        <span class="display-no display-md gal-no">0{unit.data.order}</span>
        <div>
          <div class="kicker">APARTMENT #{unit.data.order}</div>
          <h2 class="gal-title">{unit.data.name_en ?? unit.data.name}</h2>
          <a href={localizedPath(`/unidades/${unit.slug.replace(/^en\//, '')}`, lang)} class="link-acc gal-detail-link">{t('galleryPage.viewDetail', lang)}</a>
        </div>
        <span class="spec-mono gal-meta">{unit.data.gallery.length} PHOTOS · {unit.data.specs.metros}</span>
      </header>
      <div class="gal-grid">
        {(() => {
          const mids = midPositions(unit.data.gallery.length);
          const groupName = `unit-${unit.data.order}`;
          return unit.data.gallery.map((img, i) => {
            const cls = i === 0 ? 'gal-cell-lead' : mids.has(i) ? 'gal-cell-mid' : '';
            return (
              <figure class={`gal-cell ${cls}`}>
                <a href={img.src} class="gal-link" data-lb-group={groupName} data-lb-index={i} data-lb-caption={img.caption ?? ''} aria-label={img.caption ?? `Photo ${i + 1}`}>
                  <img src={img.src} alt={img.caption ?? `${unit.data.name_en ?? unit.data.name} — photo ${i + 1}, apartment in Capilla del Monte`} loading="lazy" decoding="async" width="1600" height="1200" />
                </a>
                {img.caption && <figcaption class="gal-cap">{img.caption}</figcaption>}
              </figure>
            );
          });
        })()}
      </div>
    </section>
  ))}

  <CTABlock lang={lang} />
  <Lightbox lang={lang} />
</Base>

<style>
  .gal-section {
    border-bottom: 1.5px solid var(--mf-ink);
    background: var(--mf-paper);
  }
  .gal-head {
    display: grid;
    grid-template-columns: auto 1fr auto;
    align-items: baseline;
    gap: 24px;
    padding: 30px 28px 22px;
    border-bottom: 1px solid var(--mf-hair);
  }
  .gal-title {
    font-size: 32px;
    line-height: 1;
    letter-spacing: -0.025em;
    font-weight: 700;
    margin-top: 6px;
  }
  .gal-meta { letter-spacing: 0.1em; }
  .gal-detail-link {
    display: inline-block;
    margin-top: 8px;
    font-size: 13px;
    font-weight: 500;
  }

  .gal-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0;
    background: var(--mf-stone);
  }
  .gal-cell {
    position: relative;
    aspect-ratio: 1 / 1;
    margin: 0;
    overflow: hidden;
    border-right: 1px solid var(--mf-hair);
    border-bottom: 1px solid var(--mf-hair);
  }
  .gal-cell-lead {
    grid-column: span 2;
    grid-row: span 2;
    aspect-ratio: auto;
  }
  .gal-cell-mid {
    grid-row: span 2;
    aspect-ratio: auto;
  }
  .gal-link {
    display: block;
    width: 100%;
    height: 100%;
    cursor: zoom-in;
  }
  .gal-cell img { width: 100%; height: 100%; object-fit: cover; transition: transform 0.4s ease; }
  .gal-link:hover img { transform: scale(1.025); }
  .gal-cap {
    position: absolute;
    left: 12px;
    bottom: 12px;
    padding: 5px 9px;
    background: rgba(27, 26, 22, 0.6);
    backdrop-filter: blur(8px);
    color: var(--mf-paper);
    font-family: var(--mf-font-mono);
    font-size: 9.5px;
    font-weight: 500;
    letter-spacing: 0.14em;
    text-transform: uppercase;
  }

  @media (max-width: 900px) {
    .gal-grid { grid-template-columns: 1fr 1fr; }
    .gal-cell-lead { grid-column: span 2; grid-row: span 1; aspect-ratio: 4 / 3; }
    .gal-cell-mid { grid-column: span 1; grid-row: span 2; aspect-ratio: auto; }
  }
  @media (max-width: 720px) {
    .gal-head { grid-template-columns: 1fr; padding: 20px 18px 16px; gap: 10px; }
    .gal-title { font-size: 22px; }
    .gal-meta { text-align: left; }
    .gal-grid { grid-template-columns: 1fr; }
    .gal-cell-lead, .gal-cell-mid {
      grid-column: span 1;
      grid-row: span 1;
      aspect-ratio: 4 / 3;
    }
  }
</style>
